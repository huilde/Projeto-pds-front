import styled from "styled-components"

const StyledFilms = styled.div`
    .ant-card-body {
        padding: 0px;
    }

    .ant-pagination-options {
        display: none;
    }

    .ant-image-mask {
        border-radius: 8px;
    }
`

export { StyledFilms }